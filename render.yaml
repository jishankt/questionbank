services:
  - type: web
    name: questionbank
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn questionbank.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11